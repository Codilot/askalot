development:
  adapter: async

test:
  adapter: async

production:
  #adapter: redis
  #url: redis://localhost:6379/1
  adapter: postgresql
  url: ENV['DATABASE_URL']
  channel_prefix: chatalot_production
