
<%= form_for @user, url: {controller: "users", action: "update_expertise", params: {id: @user.id}} do |form| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row justify-content-center">
  <div class="form-group col-10">
    <%= form.select :expertise, ["Expert", "Senior", "Junior", "Newbie"], {}, {class: 'form-control'} %>
  </div>
  </div>


  <div class="actions">
    <%= form.submit "Go", class: "btn btn-outline-primary btn-lg" %>
  </div>
<% end %>
